server:
  port: 8080

spring:
  datasource:
    url: jdbc:oracle:thin:@auth-db:1521/FREEPDB1
    username: system
    password: "0000"
    driver-class-name: oracle.jdbc.OracleDriver

  jpa:
    hibernate:
      ddl-auto: update
    properties:
      hibernate:
        dialect: org.hibernate.dialect.OracleDialect

  redis:
    host: auth-redis
    port: 6379

eureka:
  client:
    service-url:
      defaultZone: http://eureka-server:8761/eureka
